{
  "name": "personal-center-android",
  "private": true,
  "version": "0.1.0",
  "description": "PERSONAL CENTER Android shell with native lock-screen notifications",
  "scripts": {
    "android:add": "npx cap add android",
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "doctor": "npx cap doctor"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/local-notifications": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.9.0"
  }
}
